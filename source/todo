complain if shader is not where we expect thems to be
make the input affect some button_down struct, act on that per frame
put world state in a make_unique or so so we can init it with N at run/starttime
